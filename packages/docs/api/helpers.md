# API · 工具函数

- `icss(theme, factory)` — 一行 shortcut
- `cx(...classes)` — emotion cx 透传
- `ikeyframes(builder)` — keyframes 名称
- `injectGlobal(styles)` — 全局样式
- `resolveTheme(schema)` — 解析 function token
- `mergeTheme(parent, partial)` — 局部覆盖
