# 介绍

`zui` 是一个**框架无关**的 CSS-in-JS 工具库。

核心是 `class Chain<TSchema>`：用户定义自己的 `ThemeSchema`，就拿到完整 IDE 补全的
`s.color._primary` / `s.padding.px(16)` / `s.color('red')` 等**四态访问**。

> 设计目标详见仓库根 [`Plan.md`](https://github.com/kenconnet666/zui/blob/main/Plan.md)。
